name: Github Pages Deployment
on:
    push:
        branches: [master]

jobs:
    build:
        runs-on: ubuntu-latest

        steps:
            - name: checkout
              uses: actions/checkout@v2

            - name: npm install
              run: npm install

            - name: npm run deploy
              run: npm run deploy
